node_modules/
.expo/
.env
expo-start.log
expo-start.err.log

# Native projects are generated by `expo prebuild` (CNG) — do not commit them
/ios/
/android/

# Secrets / signing material — NEVER commit these
credentials/
credentials.json
*.jks
*.keystore
*.p8
*.p12
*.mobileprovision
# Google service-account keys and Firebase config files
anklepath-*.json
google-services.json
GoogleService-Info.plist
